[project]
name = "rag-hybrid-search"
version = "0.1.0"
description = "Production-grade RAG pipeline with hybrid (dense + BM25) search over internal docs"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 120
target-version = "py312"
