{
  "name": "rag-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'No lint configured yet' && exit 0",
    "test": "echo 'No tests configured yet' && exit 0",
    "format": "echo 'No format configured yet' && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.2",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}